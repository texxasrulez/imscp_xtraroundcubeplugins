<?php

// URLs to nextCloud instance (use same url as rc to avoid cross domains problems) 
// Trailing Slash Requred
$rcmail_config['nextcloud_url'] = '{nextcloud_url}';

// DES key for password
$rcmail_config['roundcube_nextcloud_des_key'] = '{roundcube_nextcloud_des_key}';
